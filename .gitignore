# Operator data — never committed. Contains the personal match database and
# every archived screenshot.
data/

# Everything below stays on this machine. The published repository is the
# application and nothing else: no screenshots of real lobbies, no personal
# notes, no working documents, no test fixtures naming real players.
# All of it is still in the local `master` branch history.
samples/
tests/
tools/
updates.md
CLAUDE-OWTRACKER.md

# Build output
dist/
*.zip

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Session handoff (working file, not versioned)
RESUME.md
